<tr>
  <td><%= post.id %></td>
  <td><%= post.user.name%></td>
  <td><%= post.title %></td>
  <td><div id="overflow"><%= post.content.html_safe %></div></td>
  <td><%= post.place.name %></td>
  <td><%= link_to "Details", admin_post_path(post) %>/
  <%= link_to "Delete", admin_post_path(post), method: :delete %></td>
</tr>
